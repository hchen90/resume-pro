## 1. Month input + year coercion

- [x] 1.1 Add `toMonthInputValue` helper with unit tests
- [x] 1.2 Restore `type="month"` in `NodeEditor` using the helper for display values
- [x] 1.3 Update docs/prompts for month picker + year-only coercion
- [x] 1.4 Add a persistent form-control constraint rule for resume editor components

## 2. Verification

- [x] 2.1 Run tests / coverage for touched AI and resume modules
