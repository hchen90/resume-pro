"use client";

import "@/lib/resume/load-bundled-fonts";

/** Ensures bundled @font-face rules are included in the client bundle for resume views. */
export function BundledResumeFonts() {
  return null;
}
